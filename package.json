{
  "name": "chat-app",
  "version": "1.0.0",
  "description": "Chat application using react and expressjs",
  "author": "",
  "license": "MIT",
  "private": "true",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "start:web": "yarn lerna run dev --scope=@smohk/web",
    "start:server": "yarn lerna run dev --scope=@smohk/server"
  },
  "devDependencies": {
    "lerna": "^7.1.4"
  }
}
